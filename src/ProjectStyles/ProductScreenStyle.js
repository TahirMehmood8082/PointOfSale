import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  
  productScreenSafeArea: {
    flex: 1, 
    backgroundColor: "white", 
    alignItems: "center",
  },
});

export default styles;